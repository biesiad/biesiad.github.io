<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 450" width="800" height="450">
  <rect width="800" height="450" fill="#f5f5f5"/>

  <!-- Progress bar track -->
  <rect x="100" y="185" width="600" height="24" rx="12" fill="#e0e0e0"/>

  <!-- Progress bar fill — stuck at ~40% -->
  <rect x="100" y="185" width="240" height="24" rx="12" fill="#111111"/>

  <!-- Label -->
  <text x="400" y="270" font-family="system-ui, sans-serif" font-size="18" fill="#aaaaaa" text-anchor="middle" letter-spacing="2">loading...</text>
</svg>
